import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        justifyContent: 'center',
        paddingTop: 120,
        alignItems: 'center',
    },
    logo: {
        height: 150,
        maxWidth: '60%',
        resizeMode: 'contain',
    },
})
